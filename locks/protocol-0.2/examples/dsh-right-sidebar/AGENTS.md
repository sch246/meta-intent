# Agent entry

This host contains an embedded intent package at `.intent/`.

- Read `.intent/state/STATE.json` and its selected protocol first.
- The host may contain a native DSH plugin layout outside `.intent/`; those files are target reality, not package authority.
- No realization lock is selected, so this example does not claim reproducible installation or lifecycle acceptance.
