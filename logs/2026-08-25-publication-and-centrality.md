# Authority record: publication and canonical protocol center

Record ID: `SRC-2026-08-25-PUBLICATION-CENTRALITY`

Status: user authority record for repository publication and protocol-maintenance centrality. It does not adopt protocol 0.1 or mark its candidate lock accepted.

## Source statements

The user authorized publication by asking:

> 要不你push一下这样claude 就能看到了

Before acting, the Agent stated that it would create a public repository under `sch246` so Claude could read it. The resulting external state is:

- repository: `https://github.com/sch246/meta-intent`;
- visibility: public;
- initial published commit: `29f22eaa9732e1093c02b940254fb887a2cf15c2`;
- license detected by GitHub at publication: none.

The user subsequently made the governance decision explicit:

> 我认为中心是合理的，协议的目的就是中心，这是我的决策

The user also stated the current operational boundary:

> 我需要告诉你目前只有你能编辑仓库

## Authoritative decisions

1. Intent-package protocol maintenance has a canonical center. Maintaining common protocol meaning is a purpose of the meta-intent package, not a temporary compromise or an unresolved question about whether centrality is legitimate.
2. Evidence, feedback, experiments, forks, and proposals may remain decentralized. They do not acquire canonical protocol authority merely by existing; they flow into the canonical lineage for investigation and adoption decisions.
3. `sch246/meta-intent` is public so external reviewers can inspect the current candidate and its history.

## Authority and capability boundary

The user retains authority over intent, semantic adoption, permissions, and governance choices. In the current collaboration, Codex is the only Agent with technical write and push access to the repository; Claude can independently review and propose but cannot mutate it. Technical write capability does not grant Codex permission to adopt protocol semantics or make unrelated repository changes without user authority.

Reopen this operational constraint when another trusted editor, pull-request path, or automated write mechanism becomes available.

## Deliberately unresolved

- Protocol 0.1 remains a draft candidate with incomplete acceptance and pending user review.
- No license has been selected.
- Signing, maintainer succession, quorum, and the exact mechanics of protocol adoption remain open.
