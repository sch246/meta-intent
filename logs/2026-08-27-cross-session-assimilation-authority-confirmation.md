# Cross-session assimilation authority confirmation

Record ID: `SRC-2026-08-27-CROSS-SESSION-ASSIMILATION-AUTHORITY-CONFIRMATION`

Status: explicit user authority event confirming the intent expressed by `IP-2.1` and the observable acceptance expressed by `META-015`. It confirms the semantic scope recorded in `SRC-2026-08-27-CROSS-SESSION-ASSIMILATION-CAPABILITY`; it does not claim that the capability has passed, select an implementation carrier, change protocol 0.2 or authorize publication by itself.

## Decision frame

The candidate semantics were restated in intent-oriented language:

- changing Agents must not make the project lose the maintained intent, prior occurrence, prior intervention and current continuation point;
- a later cold Agent must recover and actually use durable maintenance results without the user directing it to a particular LOG;
- duplicate reports, expected recurrence and unexpected recurrence are distinguished from checked reality rather than wording alone;
- success requires a real handoff in which the later Agent's maintenance path is changed by the preserved result.

The candidate remains representation-neutral: it does not preselect a graph, index, CLI, skill, database or runtime.

## Source statement

After reviewing that restatement, the user replied:

> 对

## Authorized decision

The user confirms that `IP-2.1` and `META-015` express the intended cross-session maintenance capability and its non-vacuous acceptance boundary.

This confirmation establishes the semantics; it does not establish that `META-015` is satisfied. Current STATE must continue to report the capability as unproved until qualifying evidence exists.
