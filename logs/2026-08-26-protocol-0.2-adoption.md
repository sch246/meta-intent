# Protocol 0.2 adoption

Record ID: `SRC-2026-08-26-PROTOCOL-0.2-ADOPTION`

Status: explicit user authority event adopting protocol 0.2 for the canonical `sch246/meta-intent` lineage. It supersedes the earlier candidate-only status; it does not rewrite the records made while adoption was still pending.

## Source statement

The user stated:

> 采用 protocol 0.2

## Authorized decision

Protocol 0.2, implemented by `locks/protocol-0.2/PROTOCOL.md` and its sealed bundle, is adopted as the current canonical interpreter for this meta-intent lineage.

The protocol lock may therefore be marked accepted, carry this decision as its authority event, and move from `candidateLocks` to `activeLocks`.

## Scope and retained boundaries

This decision adopts the protocol revision with its explicit known boundaries and open tensions. It does not:

- mark any right-sidebar realization accepted beyond its separately observed evidence;
- claim that signing, succession, cross-revision migration, semantic validation, composition or unavailable-cache lifecycle questions are solved;
- approve future semantic revisions of the meta-intent state or a later protocol revision in advance;
- require the evolving meta-intent state to stop at its current draft projection.

The state therefore remains an evolvable draft authority while its selected protocol realization becomes accepted and active. Later protocol changes require another candidate and another explicit adoption event.

## Realization consequence

The accepted protocol bundle becomes immutable last-good evidence for the state and provenance frozen at adoption. Routine later state evolution must not reseal or mutate that accepted bundle; a semantic protocol replacement is realized as a new candidate bundle.
