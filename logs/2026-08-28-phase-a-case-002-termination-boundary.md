# Phase A case 002 termination boundary

Record ID: `SRC-2026-08-28-PHASE-A-CASE-002-TERMINATION-BOUNDARY`

Status: post-launch user-authorized process boundary. The user observed that apparatus review was tending toward unbounded deep-search iteration and required this experiment to have an actual end. This authority arrived after the neutral controls passed and primary-1 launched, but before the evaluator or runner read any Case 002 semantic conclusion. It is not represented as pre-launch gold or used to change participant answers, fact gold, mechanical judgment, STATE, protocol, LOCK or `META-015`. This record is not selected by `state/STATE.json`.

## Evidence at closure

Claude froze `case002smokepass.tar.gz`, SHA-256 `948518e54df962e8ab95a5a087dd27fc54edf0aaa9328b694e90286d6d830da7`. Its payload manifest verifies. Trace tools v0.6 matched the preregistered archive digest and passed 44 of 44 tests. The fresh oversized-output and structured-search control was deterministic `valid` with complete sidecar, fingerprint and command-review binding coverage. The fresh real outside-root `Read` remained automatic `invalid`. The two controls were therefore distinguishable before Case 002 participant launch.

The smoke command-review artifact was runner-produced and explicitly non-independent. That limitation is acceptable only for proving the smoke mechanism operates; every formal Case 002 arm still requires the already specified blind command review. It is not a reason to invent another smoke or trace-tool version.

## Finite execution schedule

Trace tools v0.6, the frozen runner recipe and the active Case 002 commitment are final for this case. No proactive counterexample search, adversarial hardening, v0.7 apparatus, additional smoke family or apparatus-driven Case 003 is authorized.

Only the already registered schedule may run:

- primary-1, primary-2 and one full-history replay;
- at most the existing single transient process/launch retry for each arm, under the unchanged eligibility rule;
- at most one already registered direct-evidence diagnostic probe for each primary whose valid result is `partial` or `fail`. Each probe is one launch, diagnostic-only, and cannot repair or reclassify its primary.

Thus Case 002 has an absolute ceiling of eight participant launches: three initial arms, at most three eligible transient retries and at most two conditional probes. Unused capacity does not justify extra runs.

An automatic `invalid`, unresolved `indeterminate`, permission/evidence-boundary failure or other apparatus problem follows the existing disposition: use the one retry only when it meets the frozen transient criterion; otherwise mark the affected arm and, when required for comparison, Case 002 `inconclusive`. Do not repair the apparatus inside this case, restart the case under another identity, or search for a better result.

## Terminal result

After the three arms and any triggered diagnostic probes reach their frozen disposition, the evaluator releases the committed envelope, salt and unchanged fact gold, performs the registered mechanical comparison, records judgment-layer differences without hindsight grading, and appends one bounded Case 002 result record. The experiment then stops whether the result is `pass`, `partial`, `fail` or `inconclusive`.

No Case 002 result by itself satisfies `META-015`, closes the broader routing/surprise tension, authorizes a STATE change or starts Phase B. A new case, new apparatus investigation, Phase B recurrence experiment or further adversarial probe requires a new explicit user authority event after this terminal result. Curiosity, model disagreement, a newly imagined escape path or the availability of unused context is not a reopening condition.
